query Posts($limit: Int!, $cursor: String) {
  posts(cursor: $cursor, limit: $limit) {
    hasMore
    posts {
      _id
      title
      createdAt
      updatedAt
      textSnippet
      creator {
        _id
        username
      }
    }
  }
}
